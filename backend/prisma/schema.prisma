// PeerEval Prisma Schema
// Supports SQLite (local dev) and PostgreSQL (production)

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// ==========================================
// CORE TABLES (Existing)
// ==========================================

model User {
  id                 Int       @id @default(autoincrement())
  email              String    @unique
  password           String
  firstName          String    @default("") @map("first_name")
  lastName           String    @default("") @map("last_name")
  universityId       String?   @map("university_id")
  role               String    @default("student")
  mustChangePassword Int       @default(0) @map("must_change_password")
  protected          Int       @default(0)
  university         String?
  department         String?
  resetToken         String?   @map("reset_token")
  resetTokenExpires  DateTime? @map("reset_token_expires")
  createdAt          DateTime  @default(now()) @map("created_at")

  // Relations
  enrollments                ClassEnrollment[]
  instructorOf               ClassInstructor[]
  teacherOf                  Class[]               @relation("TeacherRelation")
  groupMemberships           GroupMember[]
  evaluationsGiven           Evaluation[]          @relation("EvaluatorRelation")
  evaluationsReceived        Evaluation[]          @relation("EvaluateeRelation")
  finalCommentsGiven         FinalComment[]        @relation("FinalCommentEvaluator")
  finalCommentsReceived      FinalComment[]        @relation("FinalCommentEvaluatee")
  extensions                 StudentExtension[]
  createdTemplates           EvalTemplate[]
  assignmentEvalsGiven       AssignmentEvaluation[] @relation("AssignmentEvaluator")
  assignmentEvalsReceived    AssignmentEvaluation[] @relation("AssignmentEvaluatee")
  groupEvalsGiven            GroupEvaluation[]
  papersAuthored             Paper[]                @relation("PaperAuthor")
  paperReviewsAssigned       PaperReviewAssignment[] @relation("PaperReviewer")
  teacherPaperReviews        PaperReview[]          @relation("TeacherPaperReviews")

  @@map("users")
}

model Class {
  id                    Int      @id @default(autoincrement())
  name                  String
  section               String?
  semester              String?
  teacherId             Int      @map("teacher_id")
  numPhases             Int      @default(3) @map("num_phases")
  hasFinalEvaluation    Int      @default(1) @map("has_final_evaluation")
  dueDate               String?  @map("due_date")
  dueDateTimezone       String?  @map("due_date_timezone")
  archived              Int      @default(0)
  minCommentWords       Int      @default(0) @map("min_comment_words")
  evaluationMode        String   @default("phases") @map("evaluation_mode")
  allowLate             Int      @default(1) @map("allow_late")
  lateWindowHours       Int      @default(48) @map("late_window_hours")
  includeSelfEval       Int      @default(0) @map("include_self_eval")
  showGroups            Int      @default(0) @map("show_groups")
  peerTemplateId        Int?     @map("peer_template_id")
  audienceTemplateId    Int?     @map("audience_template_id")
  selfTemplateId        Int?     @map("self_template_id")
  paperReviewTemplateId Int?     @map("paper_review_template_id")
  createdAt             DateTime @default(now()) @map("created_at")

  // Relations
  teacher       User              @relation("TeacherRelation", fields: [teacherId], references: [id])
  enrollments   ClassEnrollment[]
  instructors   ClassInstructor[]
  groups        Group[]
  evaluations   Evaluation[]
  finalComments FinalComment[]
  phaseDueDates PhaseDueDate[]
  extensions    StudentExtension[]
  criteria      ClassEvalCriterion[]
  assignments   Assignment[]

  @@map("classes")
}

model ClassEnrollment {
  id        Int      @id @default(autoincrement())
  classId   Int      @map("class_id")
  userId    Int      @map("user_id")
  createdAt DateTime @default(now()) @map("created_at")

  class Class @relation(fields: [classId], references: [id], onDelete: Cascade)
  user  User  @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([classId, userId])
  @@map("class_enrollments")
}

model ClassInstructor {
  id        Int      @id @default(autoincrement())
  classId   Int      @map("class_id")
  userId    Int      @map("user_id")
  createdAt DateTime @default(now()) @map("created_at")

  class Class @relation(fields: [classId], references: [id], onDelete: Cascade)
  user  User  @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([classId, userId])
  @@map("class_instructors")
}

model Group {
  id        Int      @id @default(autoincrement())
  name      String
  classId   Int?     @map("class_id")
  createdAt DateTime @default(now()) @map("created_at")

  class            Class?            @relation(fields: [classId], references: [id], onDelete: Cascade)
  members          GroupMember[]
  groupEvaluations GroupEvaluation[]

  @@map("groups")
}

model GroupMember {
  id      Int @id @default(autoincrement())
  groupId Int @map("group_id")
  userId  Int @map("user_id")

  group Group @relation(fields: [groupId], references: [id], onDelete: Cascade)
  user  User  @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([groupId, userId])
  @@map("group_members")
}

model Evaluation {
  id           Int       @id @default(autoincrement())
  evaluatorId  Int       @map("evaluator_id")
  evaluateeId  Int       @map("evaluatee_id")
  classId      Int?      @map("class_id")
  phase        Int
  // Legacy fixed criteria columns
  contribution  Int?
  communication Int?
  reliability   Int?
  qualityOfWork Int?     @map("quality_of_work")
  collaboration Int?
  score         Int?
  comments      String?
  createdAt     DateTime @default(now()) @map("created_at")
  updatedAt     DateTime @default(now()) @map("updated_at")

  evaluator       User                       @relation("EvaluatorRelation", fields: [evaluatorId], references: [id])
  evaluatee       User                       @relation("EvaluateeRelation", fields: [evaluateeId], references: [id])
  class           Class?                     @relation(fields: [classId], references: [id], onDelete: Cascade)
  criterionScores EvaluationCriterionScore[]
  attachments     EvaluationAttachment[]

  @@unique([evaluatorId, evaluateeId, phase, classId])
  @@map("evaluations")
}

model FinalComment {
  id          Int       @id @default(autoincrement())
  evaluatorId Int       @map("evaluator_id")
  evaluateeId Int       @map("evaluatee_id")
  classId     Int?      @map("class_id")
  comments    String?
  finalPoints Int       @default(0) @map("final_points")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @default(now()) @map("updated_at")

  evaluator User   @relation("FinalCommentEvaluator", fields: [evaluatorId], references: [id])
  evaluatee User   @relation("FinalCommentEvaluatee", fields: [evaluateeId], references: [id])
  class     Class? @relation(fields: [classId], references: [id], onDelete: Cascade)

  @@unique([evaluatorId, evaluateeId, classId])
  @@map("final_comments")
}

model PhaseDueDate {
  id        Int      @id @default(autoincrement())
  classId   Int      @map("class_id")
  phase     Int
  dueDate   String   @map("due_date")
  createdAt DateTime @default(now()) @map("created_at")

  class Class @relation(fields: [classId], references: [id], onDelete: Cascade)

  @@unique([classId, phase])
  @@map("phase_due_dates")
}

model StudentExtension {
  id              Int      @id @default(autoincrement())
  classId         Int      @map("class_id")
  userId          Int      @map("user_id")
  phase           Int
  extendedDueDate String   @map("extended_due_date")
  createdAt       DateTime @default(now()) @map("created_at")

  class Class @relation(fields: [classId], references: [id], onDelete: Cascade)
  user  User  @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([classId, userId, phase])
  @@map("student_extensions")
}

// ==========================================
// NEW TABLES - Flexible Evaluation System
// ==========================================

// Templates for reusable rubrics
model EvalTemplate {
  id          Int       @id @default(autoincrement())
  name        String
  description String?
  targetType  String    @map("target_type") // 'individual' or 'group'
  isSystem    Int       @default(0) @map("is_system")
  createdById Int?      @map("created_by")
  createdAt   DateTime  @default(now()) @map("created_at")

  createdBy User?                   @relation(fields: [createdById], references: [id])
  criteria  EvalTemplateCriterion[]

  @@map("eval_templates")
}

model EvalTemplateCriterion {
  id           Int     @id @default(autoincrement())
  templateId   Int     @map("template_id")
  name         String
  description  String?
  orderIndex   Int     @default(0) @map("order_index")
  minValue     Int     @default(1) @map("min_value")
  maxValue     Int     @default(5) @map("max_value")
  questionType String  @default("likert") @map("question_type")

  template EvalTemplate @relation(fields: [templateId], references: [id], onDelete: Cascade)

  @@map("eval_template_criteria")
}

// Custom criteria for phase-based classes
model ClassEvalCriterion {
  id           Int     @id @default(autoincrement())
  classId      Int     @map("class_id")
  name         String
  description  String?
  orderIndex   Int     @default(0) @map("order_index")
  minValue     Int     @default(1) @map("min_value")
  maxValue     Int     @default(5) @map("max_value")
  questionType String  @default("likert") @map("question_type")

  class  Class                      @relation(fields: [classId], references: [id], onDelete: Cascade)
  scores EvaluationCriterionScore[]

  @@map("class_eval_criteria")
}

model EvaluationCriterionScore {
  id           Int @id @default(autoincrement())
  evaluationId Int @map("evaluation_id")
  criterionId  Int @map("criterion_id")
  score        Int

  evaluation Evaluation         @relation(fields: [evaluationId], references: [id], onDelete: Cascade)
  criterion  ClassEvalCriterion @relation(fields: [criterionId], references: [id], onDelete: Cascade)

  @@unique([evaluationId, criterionId])
  @@map("evaluation_criterion_scores")
}

// Assignment mode tables
model Assignment {
  id          Int      @id @default(autoincrement())
  classId     Int      @map("class_id")
  name        String
  description String?
  orderIndex  Int      @default(0) @map("order_index")
  dueDate     String?  @map("due_date")
  createdAt   DateTime @default(now()) @map("created_at")

  class     Class                @relation(fields: [classId], references: [id], onDelete: Cascade)
  evalTypes AssignmentEvalType[]

  @@map("assignments")
}

model AssignmentEvalType {
  id              Int     @id @default(autoincrement())
  assignmentId    Int     @map("assignment_id")
  evalType        String  @map("eval_type") // 'peer', 'audience', 'self', 'paper_review'
  name            String
  targetType      String  @map("target_type") // 'individual' or 'group'
  weight          Float   @default(1.0)
  includeSelf     Int     @default(0) @map("include_self")
  isRequired      Int     @default(1) @map("is_required")
  minCompletion   Int?    @map("min_completion")
  requireComments Int     @default(0) @map("require_comments")
  minCommentWords Int?    @map("min_comment_words")
  allowLate       Int     @default(1) @map("allow_late")
  dueDate         String? @map("due_date")

  assignment       Assignment               @relation(fields: [assignmentId], references: [id], onDelete: Cascade)
  criteria         EvalTypeCriterion[]
  evaluations      AssignmentEvaluation[]
  groupEvaluations GroupEvaluation[]
  extensions       AssignmentExtension[]
  paperReviewRound PaperReviewRound?

  @@unique([assignmentId, evalType])
  @@map("assignment_eval_types")
}

model EvalTypeCriterion {
  id           Int     @id @default(autoincrement())
  evalTypeId   Int     @map("eval_type_id")
  name         String
  description  String?
  orderIndex   Int     @default(0) @map("order_index")
  minValue     Int     @default(1) @map("min_value")
  maxValue     Int     @default(5) @map("max_value")
  questionType String  @default("likert") @map("question_type")

  evalType    AssignmentEvalType          @relation(fields: [evalTypeId], references: [id], onDelete: Cascade)
  scores      AssignmentEvaluationScore[]
  groupScores GroupEvaluationScore[]

  @@map("eval_type_criteria")
}

model AssignmentEvaluation {
  id          Int       @id @default(autoincrement())
  evaluatorId Int       @map("evaluator_id")
  evaluateeId Int       @map("evaluatee_id")
  evalTypeId  Int       @map("eval_type_id")
  comments    String?
  submittedAt DateTime? @map("submitted_at")
  isLate      Int       @default(0) @map("is_late")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @default(now()) @map("updated_at")

  evaluator   User                              @relation("AssignmentEvaluator", fields: [evaluatorId], references: [id])
  evaluatee   User                              @relation("AssignmentEvaluatee", fields: [evaluateeId], references: [id])
  evalType    AssignmentEvalType                @relation(fields: [evalTypeId], references: [id], onDelete: Cascade)
  scores      AssignmentEvaluationScore[]
  attachments AssignmentEvaluationAttachment[]

  @@unique([evaluatorId, evaluateeId, evalTypeId])
  @@map("assignment_evaluations")
}

model AssignmentEvaluationScore {
  id           Int     @id @default(autoincrement())
  evaluationId Int     @map("evaluation_id")
  criterionId  Int     @map("criterion_id")
  score        Int?
  textResponse String? @map("text_response")

  evaluation AssignmentEvaluation @relation(fields: [evaluationId], references: [id], onDelete: Cascade)
  criterion  EvalTypeCriterion    @relation(fields: [criterionId], references: [id], onDelete: Cascade)

  @@unique([evaluationId, criterionId])
  @@map("assignment_evaluation_scores")
}

model GroupEvaluation {
  id          Int       @id @default(autoincrement())
  evaluatorId Int       @map("evaluator_id")
  groupId     Int       @map("group_id")
  evalTypeId  Int       @map("eval_type_id")
  comments    String?
  submittedAt DateTime? @map("submitted_at")
  isLate      Int       @default(0) @map("is_late")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @default(now()) @map("updated_at")

  evaluator   User                       @relation(fields: [evaluatorId], references: [id])
  group       Group                      @relation(fields: [groupId], references: [id], onDelete: Cascade)
  evalType    AssignmentEvalType         @relation(fields: [evalTypeId], references: [id], onDelete: Cascade)
  scores      GroupEvaluationScore[]
  attachments GroupEvaluationAttachment[]

  @@unique([evaluatorId, groupId, evalTypeId])
  @@map("group_evaluations")
}

model GroupEvaluationScore {
  id                Int     @id @default(autoincrement())
  groupEvaluationId Int     @map("group_evaluation_id")
  criterionId       Int     @map("criterion_id")
  score             Int?
  textResponse      String? @map("text_response")

  groupEvaluation GroupEvaluation   @relation(fields: [groupEvaluationId], references: [id], onDelete: Cascade)
  criterion       EvalTypeCriterion @relation(fields: [criterionId], references: [id], onDelete: Cascade)

  @@unique([groupEvaluationId, criterionId])
  @@map("group_evaluation_scores")
}

model AssignmentExtension {
  id              Int      @id @default(autoincrement())
  userId          Int      @map("user_id")
  evalTypeId      Int      @map("eval_type_id")
  extendedDueDate String   @map("extended_due_date")
  createdAt       DateTime @default(now()) @map("created_at")

  evalType AssignmentEvalType @relation(fields: [evalTypeId], references: [id], onDelete: Cascade)

  @@unique([userId, evalTypeId])
  @@map("assignment_extensions")
}

// ==========================================
// EMAIL TEMPLATES & REMINDERS
// ==========================================

// Reusable nudge/reminder message templates
model NudgeTemplate {
  id          Int      @id @default(autoincrement())
  name        String
  subject     String   @default("Action Required: Incomplete Evaluations")
  message     String
  isDefault   Int      @default(0) @map("is_default")
  createdById Int?     @map("created_by")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @default(now()) @map("updated_at")

  @@map("nudge_templates")
}

// Scheduled reminder configuration per class
model ReminderSchedule {
  id              Int      @id @default(autoincrement())
  classId         Int      @map("class_id")
  hoursBeforeDue  Int      @map("hours_before_due") // e.g., 24, 48, 72
  enabled         Int      @default(1)
  nudgeTemplateId Int?     @map("nudge_template_id")
  lastSentAt      DateTime? @map("last_sent_at")
  createdAt       DateTime @default(now()) @map("created_at")

  @@unique([classId, hoursBeforeDue])
  @@map("reminder_schedules")
}

// Log of sent reminder emails
model ReminderLog {
  id          Int      @id @default(autoincrement())
  classId     Int      @map("class_id")
  userId      Int      @map("user_id")
  phase       Int?
  assignmentId Int?    @map("assignment_id")
  sentAt      DateTime @default(now()) @map("sent_at")

  @@map("reminder_logs")
}

// ==========================================
// EVALUATION ATTACHMENTS (Image uploads)
// ==========================================

// Attachments for phase-based evaluations
model EvaluationAttachment {
  id           Int      @id @default(autoincrement())
  evaluationId Int      @map("evaluation_id")
  fileName     String   @map("file_name")
  mimeType     String   @map("mime_type")
  fileSize     Int      @map("file_size")
  s3Key        String   @map("s3_key")
  uploadedAt   DateTime @default(now()) @map("uploaded_at")

  evaluation Evaluation @relation(fields: [evaluationId], references: [id], onDelete: Cascade)

  @@map("evaluation_attachments")
}

// Attachments for assignment-based evaluations
model AssignmentEvaluationAttachment {
  id           Int      @id @default(autoincrement())
  evaluationId Int      @map("evaluation_id")
  fileName     String   @map("file_name")
  mimeType     String   @map("mime_type")
  fileSize     Int      @map("file_size")
  s3Key        String   @map("s3_key")
  uploadedAt   DateTime @default(now()) @map("uploaded_at")

  evaluation AssignmentEvaluation @relation(fields: [evaluationId], references: [id], onDelete: Cascade)

  @@map("assignment_evaluation_attachments")
}

// Attachments for group evaluations
model GroupEvaluationAttachment {
  id                Int      @id @default(autoincrement())
  groupEvaluationId Int      @map("group_evaluation_id")
  fileName          String   @map("file_name")
  mimeType          String   @map("mime_type")
  fileSize          Int      @map("file_size")
  s3Key             String   @map("s3_key")
  uploadedAt        DateTime @default(now()) @map("uploaded_at")

  groupEvaluation GroupEvaluation @relation(fields: [groupEvaluationId], references: [id], onDelete: Cascade)

  @@map("group_evaluation_attachments")
}

// ==========================================
// ACTIVITY LOGGING
// ==========================================

// Track user actions for teacher visibility
model ActivityLog {
  id          Int      @id @default(autoincrement())
  userId      Int      @map("user_id")
  classId     Int?     @map("class_id")
  action      String   // 'login', 'view_evaluations', 'submit_evaluation', 'update_evaluation', etc.
  details     String?  // JSON string with additional context
  ipAddress   String?  @map("ip_address")
  userAgent   String?  @map("user_agent")
  createdAt   DateTime @default(now()) @map("created_at")

  @@index([userId])
  @@index([classId])
  @@index([createdAt])
  @@map("activity_logs")
}

// ==========================================
// PAPER REVIEW SYSTEM
// ==========================================

// A paper review round within an assignment (evalType='paper_review')
model PaperReviewRound {
  id                        Int       @id @default(autoincrement())
  evalTypeId                Int       @unique @map("eval_type_id")
  status                    String    @default("submission") // 'submission', 'review', 'completed'
  submissionDeadline        String?   @map("submission_deadline")
  reviewStartedAt           DateTime? @map("review_started_at")
  reviewDeadline            String?   @map("review_deadline")
  reviewDurationHours       Int       @default(48) @map("review_duration_hours")
  feedbackReleasedAt        DateTime? @map("feedback_released_at")
  autoReleaseFeedback       Int       @default(1) @map("auto_release_feedback")
  anonymousReviews          Int       @default(1) @map("anonymous_reviews")
  requireSubmissionToReview Int       @default(1) @map("require_submission_to_review")
  createdAt                 DateTime  @default(now()) @map("created_at")
  updatedAt                 DateTime  @default(now()) @map("updated_at")

  evalType    AssignmentEvalType      @relation(fields: [evalTypeId], references: [id], onDelete: Cascade)
  papers      Paper[]
  assignments PaperReviewAssignment[]

  @@map("paper_review_rounds")
}

// A student's submitted paper
model Paper {
  id          Int      @id @default(autoincrement())
  roundId     Int      @map("round_id")
  authorId    Int      @map("author_id")
  fileName    String   @map("file_name")
  mimeType    String   @map("mime_type")
  fileSize    Int      @map("file_size")
  s3Key       String   @map("s3_key")
  submittedAt DateTime @default(now()) @map("submitted_at")
  isLate      Int      @default(0) @map("is_late")

  round          PaperReviewRound       @relation(fields: [roundId], references: [id], onDelete: Cascade)
  author         User                   @relation("PaperAuthor", fields: [authorId], references: [id])
  assignedReview PaperReviewAssignment? @relation("PaperToReview")
  teacherReviews PaperReview[]          @relation("TeacherReviews")

  @@unique([roundId, authorId])
  @@map("papers")
}

// 1:1 reviewer-author assignment
model PaperReviewAssignment {
  id         Int      @id @default(autoincrement())
  roundId    Int      @map("round_id")
  reviewerId Int      @map("reviewer_id")
  paperId    Int      @unique @map("paper_id")
  createdAt  DateTime @default(now()) @map("created_at")

  round    PaperReviewRound @relation(fields: [roundId], references: [id], onDelete: Cascade)
  reviewer User             @relation("PaperReviewer", fields: [reviewerId], references: [id])
  paper    Paper            @relation("PaperToReview", fields: [paperId], references: [id], onDelete: Cascade)
  review   PaperReview?

  @@unique([roundId, reviewerId])
  @@map("paper_review_assignments")
}

// The actual review with annotations
// Can be linked to either a student assignment OR directly to a paper (for teacher reviews)
model PaperReview {
  id              Int       @id @default(autoincrement())
  assignmentId    Int?      @unique @map("assignment_id")
  paperId         Int?      @map("paper_id")           // For teacher reviews (no assignment)
  teacherId       Int?      @map("teacher_id")         // The teacher who created this review
  overallComments String?   @map("overall_comments")
  submittedAt     DateTime? @map("submitted_at")
  createdAt       DateTime  @default(now()) @map("created_at")
  updatedAt       DateTime  @default(now()) @map("updated_at")

  assignment  PaperReviewAssignment? @relation(fields: [assignmentId], references: [id], onDelete: Cascade)
  paper       Paper?                 @relation("TeacherReviews", fields: [paperId], references: [id], onDelete: Cascade)
  teacher     User?                  @relation("TeacherPaperReviews", fields: [teacherId], references: [id])
  annotations PaperAnnotation[]

  @@unique([paperId, teacherId])  // One teacher review per paper
  @@map("paper_reviews")
}

// PDF annotations (highlights, comments, drawings)
model PaperAnnotation {
  id             Int      @id @default(autoincrement())
  reviewId       Int      @map("review_id")
  annotationType String   @map("annotation_type") // 'highlight', 'comment', 'area_comment'
  positionData   String   @map("position_data") // JSON: {page, rects/x,y/boundingRect}
  content        String?
  color          String?  @default("#ffff00")
  createdAt      DateTime @default(now()) @map("created_at")

  review PaperReview @relation(fields: [reviewId], references: [id], onDelete: Cascade)

  @@map("paper_annotations")
}
